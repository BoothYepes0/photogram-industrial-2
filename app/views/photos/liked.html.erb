<div class="row">
  <div class="col-md-6 offset-md-3">
    <h1>
      <%= @user.username %>
    </h1>

<ul class="nav nav-pills nav-justified">
  
  <li class="nav-item">
    <%= link_to "Posts", user_path(@user.username), class: "nav-link" %>
  </li>

  <li class="nav-item">
    <%= link_to "Liked Photos", liked_photos_path(@user.username), class: "nav-link" %>
  </li>

  <li class="nav-item">
    <%= link_to "Feed", user_path(@user.username), class: "nav-link" %>
  </li>

  <li class="nav-item">
    <%= link_to "Followers", user_path(@user.username), class: "nav-link" %>
  </li>

 
  <li class="nav-item">
    <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
  </li>
</ul>
  <%= current_user.like_photos.count %>

  <% current_user.like_photos.each do |photo| %>
    <%= render "photos/photo", giraffe: photo %>
  <% end %>



